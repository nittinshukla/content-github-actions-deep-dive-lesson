# This is a placeholder so actions won't default to a getting started page

name: Placeholder

#on: push
#on:
  #push: 
    #branches: [main]

#on:
  #workflow_call:

on:
    issue_comment:
      types: [created]
      
    
jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - uses: github/branch-deploy@v2.3.0
  
        id: branch-deploy
        with:
          trigger: ".deploy"
      
      - uses: actions/checkout@v3
        with:
          ref: ${{ steps.branch-deploy.outputs.ref }}

      # Do some fake "noop" deployment logic here
      # conditionally run a test deployment
      - name: fake noop deploy
        if: ${{ steps.branch-deploy.outputs.continue == 'true' && steps.branch-deploy.outputs.noop == 'true' }}
        run: echo "I am doing a fake noop deploy"
        
      # Do some fake "regular" deployment logic here
      # conditionally run a regular deployment
      - name: fake regular deploy
        if: ${{ steps.branch-deploy.outputs.continue == 'true' && steps.branch-deploy.outputs.noop != 'true' }}
        run: echo "I am doing a fake regular deploy"
      
      - name: Run a one-line script
        run: echo Hello, world nitin

      - name: nstring
        id: ns
        uses: ASzc/change-string-case-action@v2
        with:
          string: ${{ github.repository }}

      - name: print string name 
        run: |
          echo ${{ steps.ns.outputs.capitalized }}
          echo ${{ github.sha }}     
          echo sec ${{ secrets.AWS_KEY_ID }}
